name: CI-CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup python
        uses: actions/setup-python@v5
<<<<<<< HEAD

      - name: Python version
        run: python --version
=======
        with:
          python-version: "3.12"
          cache: pip

      - name: Python version
        run: python --version

      - name: Install Requierements
        run: pip install -r requirements.txt

      - name: Verify app loads
        run: python -c "from app import app"

      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Building Image
        run: docker build -t lavanyasharma07/aws-flask-app:${{ github.sha }} .

      - name: Push Docker image
        run: docker push lavanyasharma07/aws-flask-app:${{ github.sha }}  

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}

          script: |
            cd /home/ubuntu/aws-production-flask-app
            pwd
            which docker
            docker --version
            sed -i "s/^IMAGE_TAG=.*/IMAGE_TAG=${{ github.sha }}/" .env
            docker compose pull
            docker compose up -d
>>>>>>> d184149805c5b27ce7dc82d21f862aee1420366b
